install_data(
    'on-demand-cpupower.service',
    install_dir: path_systemd_unit_dir,
)

# CPU on-demand script (needs rewriting to not suck.)
install_data(
    'on-demand-cpupower-switch',
    install_dir: path_sbindir,
)